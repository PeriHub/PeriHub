stages:
  - pull
  - compose

pull_job:
  tags:
    - wsl
  stage: pull
  script:
    - cd /mnt/c/Users/f_testpa/Desktop/perihub
    - sudo git pull

compose_job:
  tags:
    - wsl
  stage: compose
  script:
    - cd /mnt/c/Users/f_testpa/Desktop/perihub
    - ls
    - docker-compose up --build -d cron
  dependencies:
    - pull_job