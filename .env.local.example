# SPDX-FileCopyrightText: 2023 PeriHub <https://github.com/PeriHub/PeriHub>
#
# SPDX-License-Identifier: Apache-2.0

# Local dev overrides for `npm run dev` (Vite).
#
# TRIAL/CLUSTER_URL/OAUTH_* used to live here too, but the frontend now
# fetches those from the backend's GET /config/public at startup instead
# (see src/lib/config.ts) - set them once in the backend's .env (see
# .env.example) rather than duplicating them here.
#
# Nothing needed here by default; add local-dev-only overrides as needed.

